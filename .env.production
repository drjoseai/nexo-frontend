# Production Environment Variables
# Last updated: 2026-01-09 (Cookie Bug Fix)
# Purpose: Configure API endpoints for production deployment on Vercel
# 
# IMPORTANT: This file MUST be committed to Git for proper CI/CD
# Vercel automatically uses .env.production for production builds

# Backend API URL (Custom Domain on Render)
# Using custom domain api.trynexo.ai instead of nexo-v2-core.onrender.com
# to enable cookie sharing between app.trynexo.ai and api.trynexo.ai subdomains
NEXT_PUBLIC_API_URL=https://api.trynexo.ai

# Frontend App URL (Vercel deployment)
NEXT_PUBLIC_APP_URL=https://app.trynexo.ai

# DO NOT add sensitive secrets here - use Vercel Dashboard for those
# These variables are exposed to the client (prefixed with NEXT_PUBLIC_)
