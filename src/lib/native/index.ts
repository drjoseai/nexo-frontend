export {
  getPlatform,
  isNative,
  isIOS,
  isAndroid,
  isWeb,
  isPluginAvailable,
  getPaymentProvider,
  getApiBaseUrl,
} from './platform';

export type { NexoPlatform, PaymentProvider } from './platform';
