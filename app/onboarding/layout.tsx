/**
 * Onboarding Layout — Clean, no sidebar, no header.
 * Only shows the onboarding flow content.
 */
export default function OnboardingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
